# Semantic Workbench Release Checklist

Date: 2026-04-12
Scope: Iteration 6 governance gates

## 1) Semantic Workbench Gates

- [x] `npm run compile` passes.
- [x] `npm run lint` passes.
- [x] `npm test` passes.
- [x] `npm run check:project-config-integrity` passes.

## 2) Fixture Pass Matrix

Seeded baseline status (automated by `check:fixture-matrix`):

- [x] required fixture directories exist.
- [x] required fixture baseline artifacts exist.
- [x] heuristic-only fixture keeps no `.hdl-helper/project.json` contract.

Validate at least the following fixture projects:

- [x] `pure_rtl_project`
- [x] `rtl_tb_sva_project`
- [x] `multi_top_project`
- [x] `heuristic_only_project`
- [x] `shared_file_project`
- [x] `filelist_narrow_project`

Evidence:

- [x] `resources/regression/FIXTURE_SANITY_REPORT_2026-04-12.md` generated by `npm run check:fixture-sanity`.

For each fixture, verify:

- [x] sources grouping
- [x] hierarchy roots
- [x] target context resolution
- [x] run resolution
- [x] diagnostics behavior

Evidence:

- [x] `resources/regression/FIXTURE_VALIDATION_REPORT_2026-04-12.md` generated by `npm run check:fixture-validation`.

## 3) Debug Commands Sanity

- [x] `HDL: Debug Current Project Classification`
- [x] `HDL: Debug Active Target Context`
- [x] `HDL: Debug Recent Runs By Target`
- [x] `HDL: Debug Toolchain Health By Profile`

Evidence:

- [x] `resources/regression/DEBUG_COMMANDS_SANITY_REPORT_2026-04-12.md` generated by `npm run check:debug-commands-sanity`.

## 4) Fallback Mode Sanity

- [x] Missing `.hdl-helper/project.json` enters heuristic compatibility mode.
- [x] Invalid `activeTarget` reports warning and resolves fallback context.
- [x] Missing target top appears as explicit diagnostics issue.
- [x] Missing filelist is reported by project-config integrity gate.

Evidence:

- [x] Unit test `Active target context debug snapshot reports invalid activeTarget fallback` in `src/test/extension.test.ts`.

## 5) Release Decision

- [x] All checklist items above are complete.
- [x] Open issues are triaged and assigned with owner/date.
- [x] Release notes include semantic workbench gate status.

Evidence:

- [x] `resources/regression/SEMANTIC_WORKBENCH_SIGNOFF_2026-04-12.md` generated by `npm run check:semantic-workbench-signoff`.
- [x] `RELEASE_NOTES_V3.2.0.md` section `Semantic Workbench Gate Status (2026-04-12)`.
